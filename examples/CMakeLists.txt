ADD_EXECUTABLE(solv solv.c)
TARGET_LINK_LIBRARIES(solv satsolverext satsolver ${DB_LIBRARY} ${RPMIO_LIBRARY} ${RPMDB_LIBRARY} ${EXPAT_LIBRARY} ${ZLIB_LIBRARY})

install(TARGETS
    solv
    DESTINATION ${BIN_INSTALL_DIR})
